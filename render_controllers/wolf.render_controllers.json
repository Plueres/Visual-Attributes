{
  "format_version": "1.8.0",
  "render_controllers": {
    "controller.render.wolf": {
      "geometry": "Geometry.default",
      "materials": [ { "*": "Material.default" } ],
      "textures": [
        "query.is_name_any('mars', 'Mars', 'MARS') && query.has_owner ? Texture.mars : (query.is_angry ? Texture.angry : (query.has_owner ? Texture.tame : Texture.default))"
      ]
    }
  }
}
